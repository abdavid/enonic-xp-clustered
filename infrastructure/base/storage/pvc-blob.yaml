apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: enonic-xp-blob-pv-claim
  namespace: enonic
spec:
  volumeName: enonic-xp-blob-pv-volume
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
