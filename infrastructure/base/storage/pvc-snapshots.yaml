apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: enonic-xp-snapshot-pv-claim
  namespace: enonic
spec:
  volumeName: enonic-xp-snapshot-pv-volume
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
