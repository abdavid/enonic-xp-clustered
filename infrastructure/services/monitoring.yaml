apiVersion: v1
kind: Service
metadata:
  name: monitoring
  namespace: enonic
  labels:
    app: xp
    type: combined
spec:
  ports:
    - port: 2609
      name: http-svc-monitoring
      targetPort: http-monitoring
  selector:
    app: xp
    type: combined
  type: LoadBalancer
